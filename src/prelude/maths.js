// Math

// Bounds

export const min = (xs) => Math.min.apply(null, xs);
export const max = (xs) => Math.max.apply(null, xs);

// Trigonemetric

export const cos = (x) => Math.cos(x);
export const sin = (x) => Math.sin(x);
